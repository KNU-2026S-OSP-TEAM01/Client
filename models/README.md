# Models

YOLO 가중치 및 학습된 모델 파일을 저장하는 디렉터리.

`.pt`, `.onnx`, `.weights` 등 모델 파일은 git에 추적되지 않음 (`.gitignore` 처리).

## 사용 모델

> YOLO fine-tuning 작업(다음 sub-issue) 이후 채워질 예정.

| 파일명 | 베이스 모델 | 학습 데이터 | mAP | 비고 |
|--------|-------------|-------------|-----|------|
| TBD    | TBD         | TBD         | TBD | TBD  |